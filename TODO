* Fix the leaks of hook to make it secure. 
ie: hooked_send from within hooked execution allow to escape the hook and invoque any method without hook
* Delete distorm dependency
* Delete libruby debuginfo dependency

